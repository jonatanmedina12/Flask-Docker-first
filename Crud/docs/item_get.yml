get:
  summary: Get an item by ID
  parameters:
    - name: item_id
      in: path
      type: integer
      required: true
  responses:
    200:
      description: Item details
      schema:
        properties:
          id:
            type: integer
          name:
            type: string
          description:
            type: string
