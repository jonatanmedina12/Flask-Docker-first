get:
  summary: Get all items
  responses:
    200:
      description: A list of items
      schema:
        type: array
        items:
          properties:
            id:
              type: integer
            name:
              type: string
            description:
              type: string
